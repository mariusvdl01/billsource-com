RewriteEngine on

# If a directory or a file exists, use it directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Comment out before live deployment
RewriteBase /admin

# Otherwise forward it to index.php
RewriteRule . index.php